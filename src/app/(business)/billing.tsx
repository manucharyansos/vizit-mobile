import { useQuery } from '@tanstack/react-query';
import { Image } from 'expo-image';
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { VizitIcon } from '@/components/vizit-icon';
import { useApp } from '@/providers/app-provider';
import { businessApi } from '@/services/api/business';
import { apiErrorMessage } from '@/services/api/client';

const copy = {
  hy: { title: 'Պլան և վճարումներ', billingStatus: 'Վճարման կարգավիճակ', currentPlan: 'Ընթացիկ պլան', subscription: 'Բաժանորդագրություն', provider: 'Վճարման մատակարար', usage: 'Օգտագործում', staff: 'Աշխատակիցներ', services: 'Ծառայություններ', locations: 'Մասնաճյուղեր', methods: 'Վճարման եղանակներ Հայաստանում', live: 'Ակտիվ', setup: 'Սպասում է merchant կարգավորմանը', planned: 'Միացման փուլում', plannedHint: 'Ցուցադրվում է նախապես, բայց վճարումը դեռ անջատված է մինչև backend ինտեգրումը։', idbank: 'IDBank / vPOS', idram: 'Idram Checkout', telcell: 'Telcell Wallet', easypay: 'EasyPay / easywallet', cards: 'Բանկային քարտեր', cardsHint: 'ArCa · Visa · Mastercard', loadError: 'Չհաջողվեց բեռնել վճարումների տվյալները', retry: 'Կրկին փորձել' },
  ru: { title: 'Тариф и платежи', billingStatus: 'Статус оплаты', currentPlan: 'Текущий тариф', subscription: 'Подписка', provider: 'Платёжный провайдер', usage: 'Использование', staff: 'Сотрудники', services: 'Услуги', locations: 'Филиалы', methods: 'Способы оплаты в Армении', live: 'Активно', setup: 'Ожидает настройки merchant', planned: 'Подключение запланировано', plannedHint: 'Метод показан заранее, но оплата отключена до завершения backend-интеграции.', idbank: 'IDBank / vPOS', idram: 'Idram Checkout', telcell: 'Telcell Wallet', easypay: 'EasyPay / easywallet', cards: 'Банковские карты', cardsHint: 'ArCa · Visa · Mastercard', loadError: 'Не удалось загрузить данные оплаты', retry: 'Повторить' },
  en: { title: 'Plan and billing', billingStatus: 'Billing status', currentPlan: 'Current plan', subscription: 'Subscription', provider: 'Payment provider', usage: 'Usage', staff: 'Staff', services: 'Services', locations: 'Locations', methods: 'Payment methods in Armenia', live: 'Active', setup: 'Waiting for merchant setup', planned: 'Integration planned', plannedHint: 'Shown in advance, but disabled until the backend integration is complete.', idbank: 'IDBank / vPOS', idram: 'Idram Checkout', telcell: 'Telcell Wallet', easypay: 'EasyPay / easywallet', cards: 'Bank cards', cardsHint: 'ArCa · Visa · Mastercard', loadError: 'Could not load billing data', retry: 'Try again' },
};
const show = (v: unknown) => v == null || v === '' ? '—' : String(v);
const ratio = (current: unknown, limit: unknown) => `${show(current)} / ${limit == null ? '∞' : show(limit)}`;
const idbankLogo = 'https://idbank.am/upload/resize_cache/webp/images/logo.webp';

type Method = { key: string; name: string; detail?: string; active: boolean; officialLogo?: string };

export default function BillingScreen() {
  const { locale, theme } = useApp();
  const c = copy[locale];
  const query = useQuery({ queryKey: ['business-billing'], queryFn: businessApi.billing, retry: false, refetchOnMount: 'always', staleTime: 0 });
  const data = query.data as Record<string, unknown> | undefined;
  const business = data?.business as Record<string, unknown> | undefined;
  const subscription = data?.subscription as Record<string, unknown> | undefined;
  const plan = subscription?.plan as Record<string, unknown> | undefined;
  const provider = data?.payment_provider as Record<string, unknown> | undefined;
  const usage = data?.usage as Record<string, unknown> | undefined;
  const providerName = String(provider?.default ?? '').toLowerCase();
  const providerMode = String(provider?.mode ?? '').toLowerCase();
  const idbankConfigured = providerName.includes('idbank');
  const idbankLive = idbankConfigured && providerMode === 'live';
  const methods: Method[] = [
    { key: 'idbank', name: c.idbank, detail: idbankLive ? c.live : c.setup, active: idbankLive, officialLogo: idbankLogo },
    { key: 'idram', name: c.idram, detail: c.planned, active: false },
    { key: 'telcell', name: c.telcell, detail: c.planned, active: false },
    { key: 'easypay', name: c.easypay, detail: c.planned, active: false },
    { key: 'cards', name: c.cards, detail: c.cardsHint, active: false },
  ];

  return <SafeAreaView style={[styles.screen, { backgroundColor: theme.background }]}><ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}><Text style={[styles.title, { color: theme.text }]}>{c.title}</Text>{query.isLoading ? <ActivityIndicator color={theme.plum} /> : query.isError ? <View style={[styles.error, { backgroundColor: theme.surfaceRaised, borderColor: theme.border }]}><Text style={{ color: theme.danger, fontWeight: '900' }}>{c.loadError}</Text><Text style={{ color: theme.muted, fontSize: 12 }}>{apiErrorMessage(query.error)}</Text><Pressable onPress={() => query.refetch()}><Text style={{ color: theme.plum, fontWeight: '900' }}>{c.retry}</Text></Pressable></View> : <><View style={[styles.card, { backgroundColor: theme.surfaceRaised, borderColor: theme.border }]}><Text style={[styles.label, { color: theme.muted }]}>{c.billingStatus}</Text><Text style={[styles.value, { color: theme.text }]}>{show(business?.billing_status)}</Text><Text style={[styles.label, { color: theme.muted }]}>{c.currentPlan}</Text><Text style={[styles.value, { color: theme.plum }]}>{show(plan?.name ?? plan?.code)}</Text><Text style={[styles.label, { color: theme.muted }]}>{c.subscription}</Text><Text style={[styles.value, { color: theme.text }]}>{show(subscription?.status)}</Text><Text style={[styles.label, { color: theme.muted }]}>{c.provider}</Text><Text style={[styles.value, { color: theme.text }]}>{show(provider?.default)} · {show(provider?.mode)}</Text></View><View style={[styles.card, { backgroundColor: theme.surfaceRaised, borderColor: theme.border }]}><Text style={[styles.sectionTitle, { color: theme.text }]}>{c.usage}</Text><UsageRow label={c.staff} value={ratio(usage?.active_staff, usage?.staff_limit)} /><UsageRow label={c.services} value={ratio(usage?.services_count, usage?.services_limit)} /><UsageRow label={c.locations} value={ratio(usage?.locations_count, usage?.locations_limit)} /></View><Text style={[styles.sectionTitle, { color: theme.text, marginTop: 7 }]}>{c.methods}</Text><View style={styles.methods}>{methods.map((method) => <View key={method.key} style={[styles.method, { backgroundColor: theme.surfaceRaised, borderColor: method.active ? theme.success : theme.border }]}><View style={[styles.brandBox, { backgroundColor: method.active ? theme.successSoft : theme.plumSoft }]}>{method.officialLogo ? <Image source={{ uri: method.officialLogo }} style={styles.brandLogo} contentFit="contain" /> : <Text numberOfLines={1} style={[styles.brandText, { color: theme.text }]}>{method.name}</Text>}</View><View style={styles.methodBody}><Text style={[styles.methodName, { color: theme.text }]}>{method.name}</Text><View style={styles.methodStatus}><View style={[styles.statusDot, { backgroundColor: method.active ? theme.success : theme.muted }]} /><Text style={{ color: method.active ? theme.success : theme.muted, fontWeight: '800', fontSize: 12 }}>{method.detail}</Text></View></View><View style={[styles.lock, { backgroundColor: method.active ? theme.successSoft : theme.plumSoft }]}><VizitIcon ios={method.active ? 'checkmark.circle.fill' : 'lock.fill'} android={method.active ? 'check_circle' : 'lock'} color={method.active ? theme.success : theme.muted} size={19} /></View></View>)}</View><Text style={[styles.note, { color: theme.muted, backgroundColor: theme.plumSoft }]}>{c.plannedHint}</Text></>}</ScrollView></SafeAreaView>;
}
function UsageRow({ label, value }: { label: string; value: string }) { const { theme } = useApp(); return <View style={styles.usageRow}><Text style={{ color: theme.muted, fontWeight: '700' }}>{label}</Text><Text style={{ color: theme.text, fontWeight: '900' }}>{value}</Text></View>; }
const styles = StyleSheet.create({ screen: { flex: 1 }, content: { padding: 18, gap: 12, paddingBottom: 40 }, title: { fontSize: 28, fontWeight: '900', marginBottom: 8 }, card: { borderWidth: 1, borderRadius: 11, padding: 16, gap: 7 }, label: { fontSize: 11, fontWeight: '800', textTransform: 'uppercase', marginTop: 5 }, value: { fontSize: 18, fontWeight: '900' }, sectionTitle: { fontSize: 18, fontWeight: '900', marginBottom: 5 }, usageRow: { minHeight: 38, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 12 }, methods: { gap: 9 }, method: { minHeight: 76, borderWidth: 1, borderRadius: 11, padding: 10, flexDirection: 'row', alignItems: 'center', gap: 10 }, brandBox: { width: 92, height: 48, borderRadius: 8, alignItems: 'center', justifyContent: 'center', overflow: 'hidden', paddingHorizontal: 6 }, brandLogo: { width: 82, height: 38 }, brandText: { fontSize: 11, fontWeight: '900', textAlign: 'center' }, methodBody: { flex: 1 }, methodName: { fontSize: 14, fontWeight: '900' }, methodStatus: { flexDirection: 'row', alignItems: 'center', gap: 5, marginTop: 5 }, statusDot: { width: 7, height: 7, borderRadius: 4 }, lock: { width: 38, height: 38, borderRadius: 9, alignItems: 'center', justifyContent: 'center' }, note: { padding: 14, borderRadius: 10, lineHeight: 20 }, error: { borderWidth: 1, borderRadius: 10, padding: 14, gap: 8 } });