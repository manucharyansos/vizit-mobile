import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { router } from 'expo-router';
import { useState } from 'react';
import { ActivityIndicator, Alert, Pressable, ScrollView, StyleSheet, Switch, Text, TextInput, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { VizitIcon } from '@/components/vizit-icon';
import { useApp } from '@/providers/app-provider';
import { businessApi, BusinessLocation } from '@/services/api/business';
import { apiErrorMessage } from '@/services/api/client';

type Settings = { locations?: BusinessLocation[]; location_limit?: number };
const copy = { hy: { title: 'Հասցեներ', add: 'Ավելացնել հասցե', name: 'Մասնաճյուղի անուն', address: 'Հասցե', city: 'Քաղաք', phone: 'Հեռախոս', primary: 'Հիմնական հասցե', save: 'Պահպանել', cancel: 'Չեղարկել', delete: 'Ջնջել', limit: 'Հասցեների սահմանաչափը լրացել է', confirm: 'Ջնջե՞լ այս հասցեն։' }, ru: { title: 'Адреса', add: 'Добавить адрес', name: 'Название филиала', address: 'Адрес', city: 'Город', phone: 'Телефон', primary: 'Основной адрес', save: 'Сохранить', cancel: 'Отмена', delete: 'Удалить', limit: 'Достигнут лимит адресов', confirm: 'Удалить этот адрес?' }, en: { title: 'Locations', add: 'Add location', name: 'Location name', address: 'Address', city: 'City', phone: 'Phone', primary: 'Primary location', save: 'Save', cancel: 'Cancel', delete: 'Delete', limit: 'Location limit reached', confirm: 'Delete this location?' } };
const empty = { name: '', address: '', city: '', phone: '', is_primary: false };
export default function Locations() {
  const { locale, theme } = useApp(); const c = copy[locale]; const qc = useQueryClient(); const query = useQuery<Settings>({ queryKey: ['business-settings'], queryFn: businessApi.settings });
  const [editing, setEditing] = useState<number | 'new' | null>(null); const [form, setForm] = useState(empty);
  const refresh = () => qc.invalidateQueries({ queryKey: ['business-settings'] }); const fail = (e: unknown) => Alert.alert(c.title, apiErrorMessage(e));
  const save = useMutation({ mutationFn: () => editing === 'new' ? businessApi.createLocation({ ...form, is_active: true }) : businessApi.updateLocation(editing!, form), onSuccess: () => { setEditing(null); setForm(empty); void refresh(); }, onError: fail });
  const remove = useMutation({ mutationFn: businessApi.deleteLocation, onSuccess: () => void refresh(), onError: fail });
  const edit = (item: BusinessLocation) => { setEditing(item.id); setForm({ name: item.name ?? '', address: item.address ?? '', city: item.city ?? '', phone: item.phone ?? '', is_primary: item.is_primary }); };
  const locations = query.data?.locations ?? []; const canAdd = locations.length < (query.data?.location_limit ?? 1);
  return <SafeAreaView style={[styles.screen, { backgroundColor: theme.background }]}><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><View style={styles.header}><Pressable onPress={() => router.back()} style={[styles.back, { borderColor: theme.border }]}><VizitIcon ios="chevron.left" android="arrow_back" color={theme.text} size={21} /></Pressable><Text style={[styles.title, { color: theme.text }]}>{c.title}</Text></View>{query.isLoading && <ActivityIndicator color={theme.plum} />}{locations.map((item) => <View key={item.id} style={[styles.card, { borderColor: theme.border, backgroundColor: theme.surfaceRaised }]}><View style={[styles.pin, { backgroundColor: theme.plumSoft }]}><VizitIcon ios="mappin.and.ellipse" android="location_on" color={theme.plum} size={22} /></View><Pressable style={{ flex: 1 }} onPress={() => edit(item)}><Text style={{ color: theme.text, fontWeight: '900', fontSize: 15 }}>{item.name || item.address || '—'}</Text><Text style={{ color: theme.muted, marginTop: 4 }}>{item.address}{item.city ? `, ${item.city}` : ''}</Text>{item.is_primary && <Text style={{ color: theme.plum, fontSize: 11, fontWeight: '900', marginTop: 5 }}>{c.primary}</Text>}</Pressable><Pressable onPress={() => Alert.alert(c.delete, c.confirm, [{ text: c.cancel }, { text: c.delete, style: 'destructive', onPress: () => remove.mutate(item.id) }])}><VizitIcon ios="trash" android="delete" color={theme.danger} size={20} /></Pressable></View>)}{editing && <View style={[styles.editor, { borderColor: theme.plum, backgroundColor: theme.surfaceRaised }]}>{(['name', 'address', 'city', 'phone'] as const).map((key) => <TextInput key={key} value={form[key]} onChangeText={(v) => setForm((f) => ({ ...f, [key]: v }))} placeholder={c[key]} placeholderTextColor={theme.muted} keyboardType={key === 'phone' ? 'phone-pad' : 'default'} style={[styles.input, { color: theme.text, borderColor: theme.border, backgroundColor: theme.background }]} />)}<View style={styles.toggle}><Text style={{ color: theme.text, fontWeight: '800' }}>{c.primary}</Text><Switch value={form.is_primary} onValueChange={(v) => setForm((f) => ({ ...f, is_primary: v }))} trackColor={{ false: theme.border, true: theme.plum }} /></View><View style={styles.actions}><Pressable onPress={() => setEditing(null)} style={[styles.secondary, { borderColor: theme.border }]}><Text style={{ color: theme.text, fontWeight: '800' }}>{c.cancel}</Text></Pressable><Pressable disabled={!form.address.trim()} onPress={() => save.mutate()} style={[styles.primary, { backgroundColor: theme.plum, opacity: form.address.trim() ? 1 : 0.4 }]}><Text style={styles.white}>{c.save}</Text></Pressable></View></View>}{!editing && <Pressable disabled={!canAdd} onPress={() => { setForm(empty); setEditing('new'); }} style={[styles.add, { borderColor: theme.plum, opacity: canAdd ? 1 : 0.45 }]}><VizitIcon ios="plus" android="add" color={theme.plum} size={20} /><Text style={{ color: theme.plum, fontWeight: '900' }}>{canAdd ? c.add : c.limit}</Text></Pressable>}</ScrollView></SafeAreaView>;
}
const styles = StyleSheet.create({ screen: { flex: 1 }, content: { padding: 18, paddingBottom: 44, gap: 10 }, header: { flexDirection: 'row', alignItems: 'center', gap: 12, marginBottom: 8 }, back: { width: 43, height: 43, borderWidth: 1, borderRadius: 9, alignItems: 'center', justifyContent: 'center' }, title: { fontSize: 26, fontWeight: '900' }, card: { borderWidth: 1, borderRadius: 10, minHeight: 88, padding: 12, flexDirection: 'row', alignItems: 'center', gap: 11 }, pin: { width: 44, height: 44, borderRadius: 9, alignItems: 'center', justifyContent: 'center' }, editor: { borderWidth: 1, borderRadius: 10, padding: 13, gap: 9 }, input: { height: 50, borderWidth: 1, borderRadius: 8, paddingHorizontal: 12 }, toggle: { flexDirection: 'row', minHeight: 46, alignItems: 'center', justifyContent: 'space-between' }, actions: { flexDirection: 'row', gap: 8 }, secondary: { flex: 1, height: 50, borderWidth: 1, borderRadius: 8, alignItems: 'center', justifyContent: 'center' }, primary: { flex: 1, height: 50, borderRadius: 8, alignItems: 'center', justifyContent: 'center' }, white: { color: '#FFF', fontWeight: '900' }, add: { minHeight: 55, borderWidth: 1, borderRadius: 9, borderStyle: 'dashed', flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 7, padding: 8 } });
